<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <!-- namespace는 Mapper 인터페이스의 경로를 그대로 써준다. -->
<mapper namespace="com.koreait.mapper.TimeMapper">
	<!-- com.koreait.mapper.TimeMapper.getTime2 -->
	<!-- 
		id 속성값은 쿼리를 사용할 때 호출할 메소드의 이름과 동일하게 맞추어야 한다.
		resultType 값은 인터페이스에 선언된 메소드의 리턴 타입과 동일하게 작성한다.
	 -->
	<select id="getTime2" resultType="string">
		select now() from dual
	</select>
</mapper>